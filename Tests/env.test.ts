// test("reads envs from .env file", () => {
//   expect(import.meta.env.VITE_DEBUG_MODE).toBe("true");
// });

describe("A truthy statement", () => {
  it("should be equal to 2", () => {
    expect(1 + 1).toEqual(2);
  });
});
